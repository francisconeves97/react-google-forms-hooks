{
  "name": "google-form-parser",
  "version": "1.0.0",
  "description": "Util that parses a Google Form into a JSON.",
  "author": "francisconeves97",
  "license": "MIT",
  "scripts": {
    "build": "NODE_ENV='production' tsup-node",
    "dev": "NODE_ENV='development' tsup-node",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "tsup": "^6.3.0",
    "vite": "^3.2.2",
    "vitest": "^0.24.5",
    "esbuild": "^0.15.12",
    "esbuild-plugin-eslint": "^0.1.1"
  }
}