{
  "name": "@gforms-js/react",
  "version": "0.0.1",
  "repository": {
    "url": "https://github.com/francisconeves97/gforms-js"
  },
  "description": "Set of utils to build Google Forms UIs using React.",
  "author": "francisconeves97",
  "license": "MIT",
  "scripts": {
    "build": "NODE_ENV='production' tsup-node",
    "dev": "NODE_ENV='development' tsup-node",
    "lint": "eslint src --ext .ts",
    "test": "vitest",
    "test:ci": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "esbuild": "^0.15.12",
    "esbuild-plugin-eslint": "^0.1.1",
    "tsup": "^6.3.0",
    "vite": "^3.2.2"
  },
  "dependencies": {
    "@gforms-js/submitter": "file:../packages/types",
    "@gforms-js/react-form-hooks": "file:../packages/react-form-hooks"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "module": "dist/index.mjs"
}
